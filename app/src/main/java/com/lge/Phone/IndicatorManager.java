package com.lge.phone;

/**
 * LG status-bar message indicator. There's no equivalent surface on Android
 * to hook this into, so it's a no-op, matching KEmulator's own behavior.
 */
public class IndicatorManager {

	public static void showMessageIndicatorIcon(boolean show) {
	}
}
